name: medical-decision-support-system
channels:
  - conda-forge
  - defaults
dependencies:
  - pandas
  - pip
  - tensorboard
  - pip:
      - tensorflow
      - tensorflow-datasets==4.4.0
      - kaggle
      - tensorflow-addons