name: medical-decision-support-system
channels:
  - apple
  - conda-forge
  - defaults
dependencies:
  - tensorflow-deps
  - pandas
  - pip
  - pip:
      - tensorflow-macos==2.7
      - tensorflow-datasets==4.4.0
      - kaggle
      - keras-tuner